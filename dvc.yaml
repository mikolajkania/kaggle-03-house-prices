stages:
  train:
    cmd: python src/pipeline/process.py params.yaml
    deps:
      - data/raw
      - src/data
      - src/pipeline/process.py
    outs:
      - data/dvc/
    metrics:
      - models/metrics.json:
          cache: false